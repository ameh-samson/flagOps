import SectionTitle from "../SectionTitle";

const Header = () => {
  return (
    <header className="px-8 py-5 bg-white drop-shadow-sm drop-shadow-black/5">
      <SectionTitle>FlagOps</SectionTitle>
    </header>
  );
};

export default Header;
