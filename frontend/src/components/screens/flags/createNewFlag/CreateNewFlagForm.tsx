import type { CreateNewFlagFormProps } from "@/types";

const CreateNewFlagForm = ({
  register,
  handleSubmit,
  onSubmit,
  errors,
  isLoading,
}: CreateNewFlagFormProps) => {
  return <div>

    
  </div>;
};

export default CreateNewFlagForm;
